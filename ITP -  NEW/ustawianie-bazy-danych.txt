Zak³adamy, ¿e na komputerze zainstalowany jest XAMPP, a wszystkie pliki projektu s¹ w folderze .../xampp/htdocs/ITP

1. Pobraæ MySQL workbench ze strony: https://dev.mysql.com/downloads/file/?id=459898
2. Wybierz SetUp New Connection
3. Connection Name: itp
4. Kliknij OK
5. Wpisz w oknie Query1:

create schema itp;

use itp;

create  table users
(
id int unsigned auto_increment primary key,
firstname varchar(30),
lastname varchar(30),
email varchar(50) not null,
password varchar(50),
add_date date,
komentarz  varchar(50)
);

create  table firmy
(
id int unsigned auto_increment primary key,
nazwa varchar(30),
opis varchar(300),
kandydaci varchar(300),
kontakt varchar(30),
email varchar(30),
password varchar(50),
add_date date,
komentarz  varchar(50)
);

6. Zaznacz wszystko i kliknij Execute (¿ó³ta b³yskawica)
7. W pliku dataBase.php zmieñ odpowiednio nazwê u¿ytkownika i has³o lub za³ó¿ nowe konto z loginem i has³em takim, jak w dataBase.php
8. Teraz mo¿na swobodnie przegl¹daæ stronê wpisuj¹c w przegl¹darce "localhost/ITP"
9. Gdy zarejestruje siê firma lub u¿ytkownik, mo¿na wyœwietliæ dane w bazie wpisuj¹c i uruchamiaj¹c nastêpuj¹ce komendy:
select * from users;
select * from firmy;